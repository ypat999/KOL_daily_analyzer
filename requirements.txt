requests>=2.31.0  # HTTP请求库

# 核心浏览器自动化库（替代原生 selenium）
selenium-wire>=5.1.0  # 兼容 Python 3.11 的最新稳定版

# Selenium 基础依赖（selenium-wire 依赖）
selenium>=4.9.1  # 与 selenium-wire 5.x 兼容的版本

# ChromeDriver 自动管理工具
webdriver-manager>=3.8.6  # 用于自动下载和管理 ChromeDriver

# HTTP 请求库
requests>=2.31.0  # 用于发送 HTTP 请求

# 解决之前遇到的 blinker 模块缺失问题
blinker==1.7.0  # 固定版本以确保_saferef模块存在（1.8+ 已移除 _saferef，selenium-wire 无法导入）
setuptools==80.9.0  # 固定版本以确保 pkg_resources 存在（setuptools 81+ 计划移除 pkg_resources，selenium-wire 依赖它）

# BeautifulSoup库，用于解析HTML
beautifulsoup4>=4.9.3  # 用于解析HTML和XML文档

# yt-dlp for video downloading
yt-dlp>=2023.11.16  # 视频下载工具

# faster-whisper for speech recognition
faster-whisper>=0.9.0  # 快速语音识别库

# 动量分析依赖
akshare>=1.12.0  # 中国股票市场数据获取（备用）
yfinance>=1.3.0  # Yahoo Finance数据获取（优先）
pandas>=2.0.0  # 数据处理
numpy>=1.24.0  # 数值计算

# OpenAI API客户端（用于DeepSeek）
openai>=1.0.0  # OpenAI API客户端

# 微信读书扫码登录二维码生成（纯Python，兼容无qrcode/Pillow环境）
segno>=1.6.0  # 纯Python二维码生成
pypng>=0.20220715.0  # segno输出PNG依赖

# 其他依赖
torch==2.6.0 --no-index -f https://mirrors.aliyun.com/pytorch-wheels/cu124/  # faster-whisper依赖
ctranslate2>=3.20.0  # faster-whisper依赖python
